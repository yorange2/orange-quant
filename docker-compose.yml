services:
  orange-quant:
    build: .
    image: orange-quant:latest
    container_name: orange-quant
    restart: unless-stopped

    env_file:
      - .env

    # 挂载模型文件（将训练好的 pkl 放到 ./models/ 目录下）
    volumes:
      - ./models:/app/models

    # 默认每日 00:15 UTC 调仓（北京时间 08:15）
    # 使用 LightGBM 模型预测，如不需要可删除 --model 参数（退化为动量策略）
    command: ["--hour", "0", "--minute", "15", "--model", "models/binance-lgb-momtopk.pkl"]

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # DRY RUN 模式（观察用，不下单）
  orange-quant-dry:
    build: .
    image: orange-quant:latest
    container_name: orange-quant-dry
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./models:/app/models
    command: ["--dry-run", "--hour", "0", "--minute", "15", "--model", "models/binance-lgb-momtopk.pkl"]
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    profiles:
      - dry-run
